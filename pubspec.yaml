name: commerchaiti
description: Application commerciale pour Haiti.
version: 1.0.0+1

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.0
  go_router: ^14.0.0
  image_picker: ^1.0.0
  flutter_image_compress: ^2.0.0
  url_launcher: ^6.0.0
  supabase_flutter: ^2.3.0
  path_provider: ^2.0.0
  shared_preferences: ^2.0.0
  pdf: ^3.10.0
  printing: ^5.12.0
  share_plus: ^7.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/